github:
  user: ${github_username}
  token: ${github_token}
  secret: ${github_secret}
webhook_ingress:
  enabled: true
  ingressClassName: "cloudflare-tunnel"
  host: k8s-homelab-atlantis-webhook.adhshr.xyz 